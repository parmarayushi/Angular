import { SortclientPipe } from './sortclient.pipe';

describe('SortclientPipe', () => {
  it('create an instance', () => {
    const pipe = new SortclientPipe();
    expect(pipe).toBeTruthy();
  });
});
